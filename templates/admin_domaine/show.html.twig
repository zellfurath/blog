{% extends 'base.html.twig' %}

{% block title %}Domaine{% endblock %}

{% block body %}
    <h1>Domaine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaine.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ domaine.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_domaine_index') }}">back to list</a>

    <a href="{{ path('admin_domaine_edit', {'id': domaine.id}) }}">edit</a>

    {{ include('admin_domaine/_delete_form.html.twig') }}
{% endblock %}
